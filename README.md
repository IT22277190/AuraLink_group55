# AuraLink_group55
AuraLink Backend connects to an MQTT broker to receive ESP32 sensor data, processes it, uses the OpenAI API to generate responses, and sends results back to the device—enabling real-time, intelligent IoT interactions in a modular, cloud-integrated system.
